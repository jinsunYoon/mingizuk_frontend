import React from 'react'
import { FlexRow, FlexColumn, SubTitle, Title, Input } from '../elements/index'

const Main = () => {
    return (
        <div>
            <Modal />
        </div>
    )
}

export default Main
